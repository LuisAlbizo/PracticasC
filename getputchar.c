#include <stdio.h>
main(){
	int a=0,b=0,c=0;
	printf("Escribe un caracter y presiona enter: ");
	a=getchar();
	printf("El caracter que escribiste fue: ");
	putchar(a);
	printf("\nEscribe otro caracter y presiona enter: ");
	b=getchar();
    printf("El caracter que es fue: ");
	putchar(b);
}
